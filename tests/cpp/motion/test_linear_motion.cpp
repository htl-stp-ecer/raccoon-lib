#include <gtest/gtest.h>
#include <gmock/gmock.h>
#include "test_support/test_fixtures.hpp"

using namespace libstp::test;

class LinearMotionTest : public MotionTestFixture {};

// TODO: Add integration tests for LinearMotion (forward axis)
TEST_F(LinearMotionTest, ForwardPlaceholder) {
    EXPECT_TRUE(true);
}

// TODO: Add integration tests for LinearMotion (lateral axis)
TEST_F(LinearMotionTest, LateralPlaceholder) {
    EXPECT_TRUE(true);
}
