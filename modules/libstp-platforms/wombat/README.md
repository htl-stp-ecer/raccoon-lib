# wombat platform

This is the platform-specific implementation for the Wombat hardware platform. It implements the necessary hardware
abstraction layer (HAL) functions to enable the library to interact with the Wombat platform.