# libstp-hal

This module provides a hardware abstraction layer (HAL) for the library. It allows for easy
integration with various hardware platforms by providing a consistent interface for operations.

This module contains the header files all platforms have to implement to be able to use the library.
It also houses the python bindings for the hal api, in case it needs to be accessed from python.