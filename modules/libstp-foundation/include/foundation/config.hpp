#ifndef LIBSTP__CONFIG_H
#define LIBSTP__CONFIG_H

#include <memory>

// Convenience umbrella include used by bindings and downstream code that only needs
// the logging entrypoints from foundation.
#include "foundation/logging.hpp"

#endif //LIBSTP__CONFIG_H
